import streamlit as st
from db.database import init_db

# Инициализация базы данных
init_db()

st.set_page_config(
    page_title="Выбор метода",
    layout="wide",
    initial_sidebar_state="expanded"
)

# Инициализация состояния
if 'selected_method' not in st.session_state:
    st.session_state.selected_method = None

# Основной контент
st.title("Выберите метод решения задачи подбора оптимальной комбинации опций компиляции")

st.markdown("""
<br>
Выберите один из методов для автоматического подбора оптимальных флагов компиляции.
Оба метода используют разные подходы к оптимизации производительности программы.
<br><br>
""", unsafe_allow_html=True)

# Контейнер для кнопок
st.markdown('<div class="buttons-container">', unsafe_allow_html=True)

# Кнопка 1: Метод Манна-Уитни
col1, col2 = st.columns([3, 1])

with col1:
    if st.button(
        "**Метод, основанный на тесте Манна-Уитни**",
        key="method_mann_whitney",
        help="Использует статистический анализ для сравнения производительности разных комбинаций флагов"
    ):
        st.session_state.selected_method = "mann_whitney"
        st.session_state.method = "mann_whitney"
        st.success("Выбран метод на основе теста Манна-Уитни")
        st.switch_page("pages/compiler_flags.py")


st.markdown("<br>", unsafe_allow_html=True)

# Кнопка 2: Генетический алгоритм
col1, col2 = st.columns([3, 1])

with col1:
    if st.button(
        "**Генетический алгоритм**",
        key="method_genetic",
        help="Использует эволюционные алгоритмы для поиска оптимальных комбинаций флагов"
    ):
        st.session_state.selected_method = "genetic"
        st.info("Метод будет доступен в следующей версии. Пока используйте метод Манна-Уитни.")
        # Для демонстрации можно раскомментировать:
        # st.session_state.method = "genetic"
        # st.switch_page("pages/compiler_flags.py")

st.markdown('</div>', unsafe_allow_html=True)

# Описание методов
st.markdown("<br><br>", unsafe_allow_html=True)

with st.expander("Подробнее о методах", expanded=False):
    col1, col2 = st.columns(2)
    
    with col1:
        st.markdown("""
        ### Метод Манна-Уитни
        **Принцип работы:**
        - Проводит серию экспериментов с разными комбинациями флагов
        - Использует непараметрический статистический тест для сравнения результатов
        - Выбирает комбинации, которые статистически значимо лучше других
        
        **Преимущества:**
        - Надёжные статистические выводы
        - Устойчив к выбросам в данных
        - Не требует предположений о распределении данных
        
        **Лучше всего подходит для:**
        - Сравнения производительности разных конфигураций
        - Поиска стабильно работающих комбинаций
        - Ситуаций с ограниченным количеством экспериментов
        """)
    
    with col2:
        st.markdown("""
        ### Генетический алгоритм
        **Принцип работы:**
        - Генерирует "популяцию" комбинаций флагов
        - Оценивает "приспособленность" каждой комбинации
        - Скрещивает лучшие комбинации и добавляет мутации
        - Эволюционирует к оптимальному решению через несколько поколений
        
        **Преимущества:**
        - Исследует большое пространство комбинаций
        - Может найти неочевидные оптимальные решения
        - Адаптируется под конкретную задачу
        
        **Лучше всего подходит для:**
        - Большого количества флагов (большое пространство поиска)
        - Многокритериальной оптимизации
        - Ситуаций, где важна скорость сходимости
        """)
