import streamlit as st
from db.database import init_db
from db.database import clear_all_data

# Инициализация базы данных
init_db()

st.set_page_config(
    page_title="Ознакомтесь с информацией о методе решения задачи",
    layout="wide",
    initial_sidebar_state="expanded"
)


# Описание методов
st.markdown("<br><br>", unsafe_allow_html=True)

with st.expander("Информация о методе решения задачи", expanded=False):
    col1, col2 = st.columns(2)
    
    with col1:
        st.markdown("""
        ### Метод Манна-Уитни
        **Принцип работы:**
        - Проводит серию экспериментов с разными комбинациями флагов
        - Использует непараметрический статистический тест для сравнения результатов
        - Выбирает комбинации, которые статистически значимо лучше других
        
        **Преимущества:**
        - Надёжные статистические выводы
        - Устойчив к выбросам в данных
        - Не требует предположений о распределении данных
        
        **Лучше всего подходит для:**
        - Сравнения производительности разных конфигураций
        - Поиска стабильно работающих комбинаций
        - Ситуаций с ограниченным количеством экспериментов
                    
        **Алгоритм автоматически выбирает метод анализа в зависимости от размера выборки:**
        #### U-тест Манна-Уитни (для малых выборок)
        - **Применяется когда:** размер экспериментальной и контрольной группы < 19
        - **Что вычисляется:** U-статистика и p-значение
        - **Интерпретация:**
        - U-статистика близкая к 0 или к максимальному значению (N1×N2) указывает на сильное различие между группами
        - p-значение < α означает статистически значимое различие
        - **Преимущества:** точный тест для малых выборок, не требует нормальности распределения
        
        #### Z-тест (на основе нормального приближения)
        - **Применяется когда:** размер экспериментальной или контрольной группы ≥ 19
        - **Что вычисляется:** z-статистика и p-значение
        - **Интерпретация:**
        - Положительная z-статистика при descending означает улучшение
        - Отрицательная z-статистика при ascending означает улучшение
        - |z| > 1.96 соответствует p < 0.05
        - **Преимущества:** быстрый расчет для больших выборок
        
        #### Выбор метода
        Алгоритм автоматически выбирает подходящий метод:
        - Если оба размера групп **меньше 19** → используется **U-тест**
        - Если хотя бы один размер группы **≥ 19** → используется **z-тест**
        """)

# Инициализация состояния
if 'selected_method' not in st.session_state:
    st.session_state.selected_method = None


# Контейнер для кнопок
st.markdown('<div class="buttons-container">', unsafe_allow_html=True)

# Кнопка 1: Метод Манна-Уитни
col1, col2 = st.columns([3, 1])

with col1:
    if st.button(
        "**Начать**",
        key="method_mann_whitney",
    ):
        st.session_state.selected_method = "mann_whitney"
        st.session_state.method = "mann_whitney"
        st.success("Выбран метод на основе теста Манна-Уитни")
        st.switch_page("pages/compiler_flags.py")